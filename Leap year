# include <stdio.h>
int main()
{
    int n;
    printf("Enter the year: ");
    scanf("%d",&n);
    if((n%100!=0) && (n%4==0))
    printf("%d is a leap year",n);
    else if(n%400==0)
    printf("\n%d is a leap year",n);
    else 
    printf("\n%d is not a leap year",n);
return 0;    
}
