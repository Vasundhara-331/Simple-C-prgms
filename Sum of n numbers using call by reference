# include <stdio.h>
int sum (int *p);
int main()
{
    int n;
    printf("Enter the limit:");
    scanf("%d",&n);
    printf("\nSum is:%d",sum(&n));
return 0;
}
int sum(int *p)
{
    int i,n,sum=0;
    for(i=0;i<*p;i++)
    {
        sum+=i;
    }
    return sum;
}
