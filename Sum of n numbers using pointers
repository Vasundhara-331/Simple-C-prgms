# include <stdio.h>
int main()
{
    int n,i,sum=0,*p;
    p=&n;
    printf("Enter the limit:");
    scanf("%d",&n);
    for(i=0;i<=*p;i++)
    {
        sum+=i;
    }
    printf("\nSum is:%d",sum);
    return 0;
}
